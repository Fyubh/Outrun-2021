// Глобальные переменные
var CAMD = 0.84;
var WIDTH;
var HEIGHT;
var ROADW = 2000;
var SEGL = 200;
var HEALTH = 1000;
var SCORE = 0;
var SPEED = 400;
var LEVEL = 1;

// Рандомные числа
function getRandomInt(max) {
    return Math.floor(Math.random() * Math.floor(max));
  }